id: 0

# Turtlebot radius ~ 0.2
# Cooky radius ~ 0.6
radius: 0.18

# Setting the Ranges in the planner code will consider the radius 
# DOF must consider robot base dimensions if the environment is surrounded by walls
# Cooky demo hallway is [10, 2.5]
DOF_min: [0,    0,  -3.14159]
DOF_max: [6.0,    6.0,  3.14159]

# Units are m/s and rad/s
max_speed_linear: 0.2
max_speed_angular: 0.75

# Units are m/s^2 and rad/s^2
max_acceleration_linear: 1.0
max_acceleration_angular: 0.5

# relative to map
start: [0, 0, 0.785]
goal:  [6.0, 6.0, 0.785]
